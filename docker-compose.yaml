services:
  ldap-nel:
    image: dethdkn/ldap-nel:1
    volumes:
      - ./database:/database
    ports:
      - 3000:3000
    environment:
      - JWT_SECRET=${JWT_SECRET}
    restart: unless-stopped
